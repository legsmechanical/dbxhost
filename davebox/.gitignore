# Build artifacts
build/
dist/
out/
bin/
obj/

# C objects / shared libs
*.o
*.obj
*.a
*.lo
*.la
*.so
*.so.*
*.dylib
*.dll
*.exe

# Debug / profiling
*.d
*.gcno
*.gcda
*.gcov
*.dSYM/
core
core.*
vgcore.*
*.log

# JavaScript / Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnp.*
.npm/
.yarn/
.cache/
.parcel-cache/

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db

# Claude local settings
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Graphify knowledge graph
graphify-out/
.graphify_ast.json

# Internal notes (not published)
notes/

# Local planning scratch
docs/plans/

# CLAUDE.md files are TRACKED since 2026-09-01 (Josh's call): the 06-25 ignore
# kept them out of the public LEGACY repo; dbxhost is public with its root
# CLAUDE.md tracked, and local-only module guidance already vanished once (the
# P1 subtree merge dropped it for six days).
# docs/superpowers/ = the Claude-Code plans + specs corpus — still local-only.
docs/superpowers/

# brainstorming visual-companion artifacts
.superpowers/

# Local ARM test binaries (built for the device; never shipped, never committed)
arm-tests/
