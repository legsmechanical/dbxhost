# Provenance for shadow_ui_param_pages.mjs in this directory.
#
# davebox OWNS that copy: it is committed and frozen, so an upstream rework of
# the host's binding cannot change the module editor underneath us. It is also
# VERBATIM, which is what keeps a future update cheap — copy the new file over,
# refresh the sha256 below, review the diff, commit. Never a merge, because the
# copy is never hand-edited (davebox's half of the seam is ui/pp_ctx.mjs).
#
# ⚠⚠ SYNC IS THE DEFAULT STATE, NOT A COURTESY. dbxhost and davebox are ONE
# repo, one deliverable, one version — the model P2/P4b enforced by deleting 378
# capability probes. A frozen copy inside that repo manufactures exactly the
# intra-repo skew that model abolishes, so the test FAILS when the host's
# binding moves and this copy does not follow. Deliberate divergence is written
# down in a sibling DIVERGED file, dated, saying what davebox needs that the
# host's binding does not do.
#
# Refresh: sha256 below is of the SOURCE file, not the copy.
#   shasum -a 256 src/shadow/shadow_ui_param_pages.mjs
#
# Checked by tests/host/test_param_pages_vendor.sh.
source: src/shadow/shadow_ui_param_pages.mjs
commit: 890fd9d19ec90ced2aae45a22b87cb923dda2e0e
sha256: 9933aac0c051c38abff19935f56af5c1a81344f81fb05e6700819f22c4d31393
date:   2026-08-31T03:42:43Z
